"""GraphRAG (Phase 2) — language-agnostic world model.

This package implements the structural + semantic graph layer that
complements the vector store. All language-specific logic lives in
queries/*.scm files; Python code here is language-agnostic.
"""
